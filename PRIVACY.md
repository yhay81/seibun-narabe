# 保存とプライバシー

食品データはブラウザへ静的ファイルとして読み込み、食品名や食品群の検索は端末内で行います。検索語をAPI、D1、ログへ送りません。

比較台へ置いた最大4食品の食品番号と1〜2,000gの量は、このブラウザの `localStorage` だけに保存します。比較内容はサーバーやURLへ送りません。「すべて外す」またはブラウザのサイトデータ削除で消去できます。

D1にはランダム端末IDのSHA-256、許可済み操作名、QA判定、時刻だけを35日保存します。検索語、食品番号、量、成分値、氏名、連絡先、IPアドレス、広告識別子の列はありません。

Cookie、外部解析、広告、アカウント認証は使いません。
